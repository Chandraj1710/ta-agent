PORT=3001
NODE_ENV=development

# LLM Provider Configuration
# Options: 'openai', 'gemini', 'claude', 'groq', 'ollama'
LLM_PROVIDER=openai
LLM_MODEL=gpt-4-turbo-preview
LLM_TEMPERATURE=0.7

# OpenAI (Paid, High Quality)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here

# Google Gemini (FREE TIER AVAILABLE!)
# Get your key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-gemini-key-here

# Anthropic Claude (FREE TIER AVAILABLE!)
# Get your key from: https://console.anthropic.com
ANTHROPIC_API_KEY=your-claude-key-here

# Groq (FREE & FAST!)
# Get your key from: https://console.groq.com
GROQ_API_KEY=your-groq-key-here

# Ollama (LOCAL & FREE - no API key needed)
# Install from: https://ollama.ai
# Just set LLM_PROVIDER=ollama

# Database (OPTIONAL - run without for in-memory alerts)
# Docker PostgreSQL: docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=postgres postgres
# Then: createdb ta_agent
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ta_agent

# Greenhouse ATS (Harvest API)
# Create at: Configure >> Dev Center >> API Credential Management
GREENHOUSE_API_KEY=your-greenhouse-api-key-here

# Referral lookback (days). 0=show all; 14=production; 3650+ for demo data (2019-2020)
# REFERRAL_LOOKBACK_DAYS=0

# Location filter: restrict to India (or other regions). Filters by application location.address OR job office name.
# Comma-separated for multiple: India,Bangalore,Mumbai
# GREENHOUSE_LOCATION_FILTER=India

# CORS
ALLOWED_ORIGINS=http://localhost:3000
